name: disable_battery_optimization_2_example
description: Demonstrates how to use the disable_battery_optimization_2 plugin.
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  disable_battery_optimization_2:
    path: ../

flutter:
  uses-material-design: true
